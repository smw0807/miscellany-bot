export const useDiscordScheduleStore = defineStore('discordSchedule', () => {
  // ============= State =============
  const state = {};

  // ============= Actions =============
  const asctions = {};

  // ============= Returns =============
  return {
    ...state,
    ...asctions,
  };
});
