export const useDiscordMessagesTriggerStore = defineStore(
  'discordMessagesTrigger',
  () => {
    // ============= State =============
    const state = {};

    // ============= Actions =============
    const actions = {};

    // ============= Return =============
    return {
      state,
      actions,
    };
  }
);
