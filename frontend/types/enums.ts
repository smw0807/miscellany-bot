export enum ResultTypeEnum {
  SUCCESS = 'success',
  ERROR = 'error',
  WARNING = 'warning',
  INFO = 'info',
}

export enum EditTypeEnum {
  ADD = 'add',
  EDIT = 'edit',
  DELETE = 'delete',
}

export enum ScheduleType {
  ONETIME = 'onetime',
  RECURRING = 'recurring',
}

export enum RepeatType {
  MINUTE = 'minute',
  HOUR = 'hour',
  DAY = 'day',
}
